create table driver (
  id int,
  name varchar(255),
  where_from varchar(255),
  where_to varchar(255),
  start_at varchar(5),
  end_at varchar(5),
  rating int,
  commution_type varchar(1),
  frequency varchar(1)
);